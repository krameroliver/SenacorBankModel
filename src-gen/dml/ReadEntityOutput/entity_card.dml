record
string("x01") card_hk
	record
	string("\x01", maximum_length=10) card_id
	string("\x01", maximum_length=10) disp_id
	string("\x01", maximum_length=100) card_type
	decimal('\x01',0) year
	decimal('\x01',0) month
	decimal('\x01',0) day
	date("YYYY-MM-DD") fulldate
end s_card;
end